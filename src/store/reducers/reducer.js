const initialState = {
    stack: [],
    currentResult: 0,
    currentInput: 0,
    displayText: 0,
    currentOperation: null,
    errorText: null
}
export const reducer = (state = initialState, action) => {
    return state;
}

export default reducer;